// Can also be included with a regular script tag
import Typed from 'typed.js';

var options = {
  strings: ['Maakai', 'Web3 Developer'],
  typeSpeed: 100,
  backspeed: 100,
  loop:true
};

var typed = new Typed('.typing', options);